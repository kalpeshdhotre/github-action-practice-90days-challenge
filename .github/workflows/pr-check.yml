name: PR Check

# Disabled pull request block for day-42 practice
on:
  workflow_dispatch:

# on:
#   pull_request:
#     branches: [main]
#     types: [opened, synchronize]

jobs:
  pr-check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Print branch info
        run: 'echo "PR check running for branch: ${{ github.head_ref }}"'
